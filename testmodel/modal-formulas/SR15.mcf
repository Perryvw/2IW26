% This formula expresses that
% RobotTakeWafer(C, p) or RobotDepositWafer(C, p) can only 
% happen if robotAccess-ingRack(B,C Rack) is false.
% R15 Robot C can not access the same spot on the rack as 
% Robot B at the same time.

% TODO