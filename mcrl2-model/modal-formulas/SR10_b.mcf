% This formula expresses that
% Robot B may only try to retrieve a wafer from an occupied sluice.

[true*] forall w:Wafer, s:SluiceID . [(!RobotToSluice(Ra, s, w))*  . RobotFromSluice(Rb, s, w)] false

%doesn't run

