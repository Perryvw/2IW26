% This formula expresses that
% A Wafer will eventually exit the system treated.

[true*] forall w: Wafer . [RobotFromInput(w) . (!TreatWafer(w))* . TreatWafer(w) . (!RobotToOutput(TreatedWafer(w)))* . RobotToOutput(TreatedWafer(w))] true

