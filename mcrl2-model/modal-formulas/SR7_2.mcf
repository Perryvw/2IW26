% This formula expresses that
% Robot A may only move to a sluice if the outer door of the target sluice is open.

[true*] forall w:Wafer, s:SluiceID . [OuterDoorClosed(s) . (!OuterDoorOpened(s))* . RobotFromSluice(Ra, s, w)] false

%Can't seen to run this one (infinite loop)
